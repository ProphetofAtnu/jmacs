;; -*- lexical-binding: t; -*-

(require 'func-util)

;; (defun alist-table-data (alists &optional regular-p)
;;   (let ((keys
;;          (if regular-p
;;              (mapcar (compose #'car #'car) alist)))))
;;   )



(provide 'tlist-util)
